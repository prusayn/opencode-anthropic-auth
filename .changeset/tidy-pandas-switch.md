---
'@ex-machina/opencode-anthropic-auth': minor
---

Add multi-account auth with configurable selection strategies (`smart` default, `lowest_quota`, `round_robin`), automatic failover on 429/401, zero-config migration of existing single-account logins, and a `scripts/accounts.ts` admin CLI
